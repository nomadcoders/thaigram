Create URL
Create View
Create template (BS) & Render
Add URL
Grab the data.
Render real data (
    user profile.
    image_set
    per image:
        like count
        comment count
)
HTML
Drop CSS.
Media querys + responsive